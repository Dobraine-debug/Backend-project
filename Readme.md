--Installation instructions--
In order to run the program, first package it by running the script "package.bat" under the "Backend-project" folder.
After that, run the script "start.bat" in order to run the program in the command line.

--Usage--
After opening the program, simply select among the available options, the program will display all results from the database on screen.